---
- heading: API references
  items:
    - label: OpenAPI specs in Doctave
      href: ./README.md

- heading: Example API
  items:
    # Here we use the special `open_api_spec` macro to generate the
    # navigation structure from the OpenAPI spec directly.
    - open_api_spec: openapi-example.yaml
